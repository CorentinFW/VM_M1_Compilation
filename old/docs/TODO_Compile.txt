═══════════════════════════════════════════════════════════════
TODO_Compile - Plan pour compléter toutes les exigences
═══════════════════════════════════════════════════════════════
Date: 11 décembre 2025
Objectif: Satisfaire 100% des exigences du projet
État actuel: 67% (4/6 exigences complètes)

═══════════════════════════════════════════════════════════════
PHASE 1: IMPLÉMENTER LABELS (fonctions locales)
═══════════════════════════════════════════════════════════════
Estimation: 2-3 heures
Priorité: HAUTE ⭐⭐⭐
Status: ❌ À FAIRE

Tâche 1.1 - Ajouter compile-labels dans compiler.lisp
──────────────────────────────────────────────────────────────
□ Parser la forme (labels ((nom params body)...) body)
□ Créer un environnement local étendu avec les noms de fonctions
□ Compiler chaque fonction label comme un DEFUN local
□ Générer les labels ASM pour chaque fonction
□ Gérer la portée lexicale correctement

Détails techniques:
  • Structure: (labels ((f1 (x) body1) (f2 (y) body2)) main-body)
  • Chaque fonction → un label ASM unique
  • Environnement étendu avant de compiler main-body
  • Les fonctions peuvent s'appeler mutuellement (récursion mutuelle)

Exemple cible:
  (labels ((even (n)
             (if (= n 0) t (odd (- n 1))))
           (odd (n)
             (if (= n 0) nil (even (- n 1)))))
    (even 10))
  → T


Tâche 1.2 - Créer les tests pour LABELS
──────────────────────────────────────────────────────────────
□ test-labels.lisp avec 5-10 tests
□ Test 1: fonction locale simple
□ Test 2: deux fonctions locales
□ Test 3: récursion mutuelle (even/odd)
□ Test 4: capture de variables externes
□ Test 5: LABELS imbriqués
□ Test 6: LABELS + closures

Fichier: test-labels.lisp (nouveau)


Tâche 1.3 - Validation et documentation
──────────────────────────────────────────────────────────────
□ Tous les tests LABELS passent
□ Mettre à jour ETAT_FINAL.txt
□ Documenter la syntaxe dans README.md

Livrables:
  ✓ compile-labels fonctionnel
  ✓ test-labels.lisp avec 10/10 tests réussis
  ✓ Documentation à jour


═══════════════════════════════════════════════════════════════
PHASE 2: MINI-LOADER EN LISP PUR (Bootstrap Niveau 3)
═══════════════════════════════════════════════════════════════
Estimation: 4-6 heures
Priorité: HAUTE ⭐⭐⭐
Status: ❌ À FAIRE

Tâche 2.1 - Créer mini-loader.lisp en LISP pur
──────────────────────────────────────────────────────────────
□ Créer fichier mini-loader.lisp
□ Parser ASM ligne par ligne (sans regexp, juste car/cdr)
□ Première passe: collecter tous les labels et leurs adresses
□ Deuxième passe: résoudre les labels et générer bytecode

Contraintes strictes:
  ✗ PAS de defstruct
  ✗ PAS de hash-table
  ✗ PAS de format avancé
  ✗ PAS de read-from-string
  ✓ Seulement: cons, car, cdr, list, append
  ✓ if, cond, let, defun, lambda
  ✓ +, -, *, /, =, <, >

Structure suggérée:
  • (defun parse-asm-line (line) ...) → (opcode arg1 arg2)
  • (defun collect-labels (lines) ...) → ((label . addr) ...)
  • (defun resolve-label (name labels) ...) → adresse
  • (defun opcode-to-byte (name) ...) → entier
  • (defun assemble (lines) ...) → liste de bytes


Tâche 2.2 - Tests mini-loader
──────────────────────────────────────────────────────────────
□ Test avec programme simple (PUSH, ADD, HALT)
□ Test avec JUMP et labels
□ Test avec CALL et fonctions
□ Comparer bytecode avec loader natif
□ Valider sur 5 programmes ASM

Fichier: test-mini-loader.lisp (nouveau)


Tâche 2.3 - Compiler et exécuter mini-loader sur VM niveau 2
──────────────────────────────────────────────────────────────
□ Compiler mini-loader.lisp → mini-loader.asm
□ Charger sur VM compilée (niveau 2)
□ Faire parser un fichier ASM simple
□ Vérifier que le bytecode est correct

Test de validation:
  VM₁ (native) → VM₂ (compilée) → mini-loader₃ (compilé)
  mini-loader₃ parse simple.asm → bytecode valide


Tâche 2.4 - Documentation bootstrap niveau 3
──────────────────────────────────────────────────────────────
□ Créer BOOTSTRAP_NIVEAU3.txt
□ Expliquer l'architecture
□ Documenter les limitations
□ Mettre à jour EXPLAIN_BOOTSTRAP.txt

Livrables:
  ✓ mini-loader.lisp fonctionnel en LISP pur
  ✓ Tests validation bytecode identique
  ✓ Mini-loader exécutable sur VM compilée
  ✓ Documentation complète


═══════════════════════════════════════════════════════════════
PHASE 3: MINI-COMPILER EN LISP PUR (Bootstrap Niveau 4)
═══════════════════════════════════════════════════════════════
Estimation: 10-15 heures
Priorité: HAUTE ⭐⭐⭐
Status: ❌ À FAIRE

Tâche 3.1 - Définir le sous-ensemble LISP à supporter
──────────────────────────────────────────────────────────────
□ Lister les formes minimales nécessaires
□ Définir les limitations acceptables
□ Documenter le sous-ensemble dans MINI_LISP_SPEC.txt

Sous-ensemble minimal:
  ✓ Nombres entiers
  ✓ Variables
  ✓ +, -, *, /, =, <, >
  ✓ if (simple, pas cond au début)
  ✓ let (1 niveau, pas imbriqué au début)
  ✓ defun (non récursif au début)
  ✓ Appels de fonction
  
Ajouts progressifs:
  → cond, progn
  → let imbriqués
  → récursion
  → lambda (si possible)


Tâche 3.2 - Créer mini-compiler.lisp en LISP pur
──────────────────────────────────────────────────────────────
□ Parser S-expressions manuellement (sans read)
□ Compiler expressions arithmétiques
□ Compiler if
□ Compiler let (simple)
□ Compiler defun
□ Compiler appels de fonction
□ Générer code ASM en texte

Contraintes strictes:
  ✗ PAS de defstruct
  ✗ PAS de hash-table
  ✗ PAS de gensym (compteurs manuels)
  ✗ PAS de format avancé
  ✓ Environnement = liste d'associations
  ✓ Compteurs passés en paramètres

Structure suggérée:
  • (defun parse-sexp (chars) ...) → sexp
  • (defun compile-expr (expr env counter) ...) → (asm new-counter)
  • (defun compile-if (expr env counter) ...) → (asm new-counter)
  • (defun compile-let (expr env counter) ...) → (asm new-counter)
  • (defun compile-defun (expr counter) ...) → (asm new-counter)
  • (defun compile-program (exprs) ...) → asm-text


Tâche 3.3 - Tests mini-compiler
──────────────────────────────────────────────────────────────
□ Test: compiler expression simple (+ 2 3)
□ Test: compiler if simple
□ Test: compiler let simple
□ Test: compiler defun simple
□ Test: compiler programme complet
□ Comparer ASM avec compiler natif

Fichier: test-mini-compiler.lisp (nouveau)

Programmes de test:
  1. (+ 2 3) → PUSH 2, PUSH 3, ADD, HALT
  2. (if (< x 5) 10 20)
  3. (let ((x 10)) (+ x 5))
  4. (defun double (x) (* x 2))
  5. Programme complet avec fonction + appel


Tâche 3.4 - Compiler mini-compiler sur VM niveau 2
──────────────────────────────────────────────────────────────
□ Compiler mini-compiler.lisp → mini-compiler.asm
□ Charger sur VM compilée
□ Compiler un programme LISP simple
□ Vérifier que l'ASM généré est correct

Test de validation:
  VM₁ → VM₂ → mini-compiler₄ (compilé)
  mini-compiler₄ compile (+ 2 3) → ASM valide


Tâche 3.5 - Documentation bootstrap niveau 4
──────────────────────────────────────────────────────────────
□ Créer BOOTSTRAP_NIVEAU4.txt
□ Documenter le sous-ensemble supporté
□ Documenter les limitations
□ Exemples de programmes compilables
□ Mettre à jour EXPLAIN_BOOTSTRAP.txt

Livrables:
  ✓ mini-compiler.lisp fonctionnel en LISP pur
  ✓ Tests validation ASM correct
  ✓ Mini-compiler exécutable sur VM compilée
  ✓ Documentation du sous-ensemble LISP
  ✓ Documentation complète niveau 4


═══════════════════════════════════════════════════════════════
PHASE 4: AUTO-HÉBERGEMENT (Bootstrap Niveau 5)
═══════════════════════════════════════════════════════════════
Estimation: 6-8 heures
Priorité: MOYENNE ⭐⭐
Status: ❌ À FAIRE

Tâche 4.1 - Préparer mini-compiler pour auto-compilation
──────────────────────────────────────────────────────────────
□ Vérifier que mini-compiler.lisp utilise seulement le sous-ensemble
□ Corriger toute dépendance externe
□ S'assurer que le code est "bootstrappable"

Validation:
  • Chaque fonction de mini-compiler doit être compilable
  • Pas de dépendance circulaire
  • Pas de feature non supportée


Tâche 4.2 - Première auto-compilation
──────────────────────────────────────────────────────────────
□ Compiler mini-compiler avec compiler natif → v1
□ Charger v1 sur VM compilée
□ Utiliser v1 pour compiler mini-compiler source → v2
□ Comparer v1 et v2 byte par byte

Test de validation:
  Compiler natif(mini-compiler.lisp) → v1.asm
  v1(mini-compiler.lisp) → v2.asm
  diff v1.asm v2.asm → IDENTIQUES ✓


Tâche 4.3 - Tests de convergence
──────────────────────────────────────────────────────────────
□ v2 compile mini-compiler → v3
□ Vérifier v2 == v3
□ v3 compile mini-compiler → v4
□ Vérifier v3 == v4
□ Convergence atteinte!

Point fixe:
  ∀n ≥ 2: vₙ == vₙ₊₁


Tâche 4.4 - Documentation auto-hébergement
──────────────────────────────────────────────────────────────
□ Créer BOOTSTRAP_NIVEAU5.txt
□ Documenter le processus complet
□ Diagrammes des niveaux
□ Preuve de convergence
□ Mettre à jour EXPLAIN_BOOTSTRAP.txt

Schéma final:
  Niveau 0: Common Lisp (host)
  Niveau 1: VM native (vm.lisp)
  Niveau 2: VM compilée (vm-bootstrap.lisp compilé)
  Niveau 3: Loader compilé (mini-loader.lisp compilé)
  Niveau 4: Compiler compilé (mini-compiler.lisp compilé)
  Niveau 5: Compiler auto-hébergé (point fixe)

Livrables:
  ✓ mini-compiler se compile lui-même
  ✓ Convergence prouvée (vₙ == vₙ₊₁)
  ✓ Documentation complète
  ✓ Système 100% autonome


═══════════════════════════════════════════════════════════════
PHASE 5: VALIDATION FINALE ET DOCUMENTATION
═══════════════════════════════════════════════════════════════
Estimation: 2-3 heures
Priorité: MOYENNE ⭐⭐
Status: ❌ À FAIRE

Tâche 5.1 - Suite de tests complète
──────────────────────────────────────────────────────────────
□ run-all-tests.lisp qui exécute:
  - run-vm-tests.lisp (11 tests)
  - run-compiler-tests.lisp (31 tests)
  - test-closures.lisp (10 tests)
  - test-labels.lisp (10 tests)
  - test-bootstrap.lisp (8 tests)
  - test-mini-loader.lisp (5 tests)
  - test-mini-compiler.lisp (10 tests)
  - test-auto-hosting.lisp (5 tests)

Total attendu: 90 tests minimum


Tâche 5.2 - Documentation utilisateur finale
──────────────────────────────────────────────────────────────
□ Mettre à jour README.md avec toutes les features
□ Guide d'utilisation complet
□ Exemples de programmes
□ Architecture complète du système

Sections:
  1. Introduction et objectifs
  2. Architecture (5 niveaux)
  3. Utilisation de la VM
  4. Utilisation du compilateur
  5. Bootstrap et auto-hébergement
  6. Limitations et extensions futures


Tâche 5.3 - Mettre à jour ETAT_FINAL.txt
──────────────────────────────────────────────────────────────
□ Marquer toutes les exigences comme ✅ COMPLÈTES
□ Statistiques finales (90+ tests)
□ Taux de complétion: 100%
□ Temps total de développement
□ Lignes de code totales


Tâche 5.4 - Créer RAPPORT_FINAL.txt
──────────────────────────────────────────────────────────────
□ Résumé exécutif du projet
□ Exigences et leur satisfaction
□ Architecture technique détaillée
□ Résultats des tests
□ Difficultés rencontrées et solutions
□ Extensions possibles

Livrables:
  ✓ Suite de tests complète (90+ tests)
  ✓ README.md professionnel
  ✓ ETAT_FINAL.txt à 100%
  ✓ RAPPORT_FINAL.txt


═══════════════════════════════════════════════════════════════
RÉCAPITULATIF DES PHASES
═══════════════════════════════════════════════════════════════

Phase │ Description                    │ Estimation │ Priorité │ Status
──────┼────────────────────────────────┼────────────┼──────────┼────────
  1   │ LABELS (fonctions locales)     │   2-3h     │   ⭐⭐⭐   │   ❌
  2   │ Mini-loader (bootstrap niv. 3) │   4-6h     │   ⭐⭐⭐   │   ❌
  3   │ Mini-compiler (bootstrap niv.4)│  10-15h    │   ⭐⭐⭐   │   ❌
  4   │ Auto-hébergement (niv. 5)      │   6-8h     │   ⭐⭐    │   ❌
  5   │ Validation et documentation    │   2-3h     │   ⭐⭐    │   ❌
──────┴────────────────────────────────┴────────────┴──────────┴────────
TOTAL │ Complétion à 100%              │  24-35h    │           │   0%


═══════════════════════════════════════════════════════════════
MÉTRIQUES DE PROGRESSION
═══════════════════════════════════════════════════════════════

État actuel:
  ✅ VM native: 100%
  ✅ Compilateur natif: 100%
  ✅ Closures: 100%
  ✅ Bootstrap niveau 2: 100%
  ❌ LABELS: 0%
  ❌ Bootstrap niveau 3: 0%
  ❌ Bootstrap niveau 4: 0%
  ❌ Bootstrap niveau 5: 0%

Tests actuels: 57/57 (100% de ce qui existe)
Tests cibles: 90+ tests
Complétion globale: 67% → objectif 100%

Exigences satisfaites: 4/6
Objectif: 6/6 (100%)


═══════════════════════════════════════════════════════════════
ORDRE D'EXÉCUTION RECOMMANDÉ
═══════════════════════════════════════════════════════════════

1. LABELS d'abord (quick win, complète exigence 4)
2. Mini-loader ensuite (base du bootstrap)
3. Mini-compiler ensuite (cœur du bootstrap)
4. Auto-hébergement (validation finale)
5. Documentation complète (finition)

Jalons clés:
  ✓ Jour 1: LABELS terminé → exigence 4 complète
  ✓ Jour 2-3: Mini-loader → bootstrap niveau 3
  ✓ Jour 4-6: Mini-compiler → bootstrap niveau 4
  ✓ Jour 7-8: Auto-hébergement → bootstrap niveau 5
  ✓ Jour 9: Documentation → projet 100% complet


═══════════════════════════════════════════════════════════════
COMMANDES UTILES POUR SUIVRE LA PROGRESSION
═══════════════════════════════════════════════════════════════

# Exécuter tous les tests actuels
clisp run-vm-tests.lisp
clisp run-compiler-tests.lisp
clisp test-closures.lisp
clisp test-bootstrap.lisp

# Vérifier les nouveaux tests (quand créés)
clisp test-labels.lisp
clisp test-mini-loader.lisp
clisp test-mini-compiler.lisp

# Compiler et tester un fichier
clisp -x '(load "compiler.lisp") (compile-to-file "test.lisp" "test.asm")'
clisp -x '(load "loader.lisp") (load-file "test.asm")'
clisp -x '(load "vm.lisp") (run-vm *code*)'

# Bootstrap niveau 2 (déjà fonctionnel)
clisp test-bootstrap.lisp


═══════════════════════════════════════════════════════════════
NOTES IMPORTANTES
═══════════════════════════════════════════════════════════════

⚠️  Le mini-compiler ne supportera qu'un SOUS-ENSEMBLE de LISP
    Limitations attendues:
    - Pas de macros
    - Pas de quote complet
    - Pas de structures de données complexes
    - Portée lexicale simple uniquement

⚠️  L'auto-hébergement nécessite que mini-compiler.lisp soit écrit
    dans son propre sous-ensemble (contrainte circulaire)

✓  Le système actuel (VM + compiler natif) reste COMPLET et FONCTIONNEL
   Le bootstrap est une EXTENSION, pas un remplacement

✓  Priorité = satisfaire les exigences utilisateur
   Le reste (macros, GC, etc.) peut être ajouté plus tard


═══════════════════════════════════════════════════════════════
PRÊT À COMMENCER!
═══════════════════════════════════════════════════════════════

Prochaine étape: Phase 1, Tâche 1.1
Action: Implémenter compile-labels dans compiler.lisp

Let's go! 🚀
